# Merge into your own ~/.tmux.conf; do not overwrite existing settings blindly.
set -g mouse on
set -g history-limit 50000
set -s set-clipboard external
# history-limit applies to newly created panes.
